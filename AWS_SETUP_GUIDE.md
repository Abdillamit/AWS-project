# Руководство по настройке AWS

## Шаг 1: Получение учетных данных AWS

Если у вас еще нет учетных данных AWS:

1. Войдите в [AWS Console](https://console.aws.amazon.com/)
2. Перейдите в **IAM** (Identity and Access Management)
3. Выберите **Users** → **Add users**
4. Создайте нового пользователя с программным доступом
5. Прикрепите политику **AdministratorAccess** (для разработки)
6. Сохраните **Access Key ID** и **Secret Access Key**

## Шаг 2: Настройка AWS CLI

Выполните команду:

```bash
aws configure
```

Введите:
- **AWS Access Key ID**: ваш ключ доступа
- **AWS Secret Access Key**: ваш секретный ключ
- **Default region name**: us-west-2 (рекомендуется)
- **Default output format**: json

## Шаг 3: Проверка настройки

```bash
aws sts get-caller-identity
```

Должен вернуть информацию о вашем аккаунте.

## Шаг 4: Bootstrap AWS CDK

```bash
cd my-project-infrastructure
export AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
cdk bootstrap aws://$AWS_ACCOUNT_ID/us-west-2
```

## Шаг 5: Развертывание

```bash
npm run deploy:beta
```

## Важные замечания

⚠️ **Безопасность**:
- Никогда не делитесь своими учетными данными
- Не коммитьте их в Git
- Используйте IAM роли с минимальными правами для продакшена

💰 **Стоимость**:
- Большинство сервисов входят в бесплатный уровень
- Ожидаемая стоимость: $0-10/месяц для разработки
- Настройте бюджетные оповещения в AWS Console

🔧 **Устранение неполадок**:
- Если `aws configure` не работает, проверьте установку AWS CLI
- Если bootstrap не работает, проверьте права IAM пользователя
- Если развертывание не удается, проверьте CloudFormation Events в консоли

## Альтернатива: Использование AWS CloudShell

Если у вас есть доступ к AWS Console, но нет локальных учетных данных:

1. Откройте [AWS CloudShell](https://console.aws.amazon.com/cloudshell)
2. Загрузите проект в CloudShell
3. Выполните команды развертывания там

## Следующие шаги

После успешного развертывания:

1. Получите выходные данные стека:
   ```bash
   aws cloudformation describe-stacks \
     --stack-name betaMyServiceAPIStack \
     --query 'Stacks[0].Outputs' \
     --output table
   ```

2. Сохраните:
   - GraphQL API URL
   - API Key
   - User Pool ID
   - User Pool Client ID

3. Настройте фронтенд с этими учетными данными
